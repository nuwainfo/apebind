<!-- Generated by APEBind. Do not edit directly. -->
# {{ distribution_name }} Node.js binding

Generated by APEBind. The package bundles `{{ binary_name }}` and invokes it
through a subprocess boundary. The generated package has no runtime npm
dependencies.

```js
import * as binding from '{{ package_name }}';
```

## WSL2

If an operation fails with `TLSError([0x6300])`, WSL may be routing the bundled
`.com` APE through Windows interop. Run the following in WSL, then restart the
WSL session:

```bash
sudo sh -c 'echo -1 > /proc/sys/fs/binfmt_misc/WSLInterop'
```
