# PHP APE -> Node.js

Generate `php-ape`, install it in a Node project, then run `run.mjs`.

The example evaluates `echo 6 * 7;` in the bundled PHP runtime and prints `42` without requiring PHP to be installed separately.
