# Python APE -> Node.js

Generate `python-ape`, install it in a Node project, then run `run.mjs`.

The example evaluates `print(6 * 7)` in the bundled Python runtime and prints `42` without requiring a system Python installation.
